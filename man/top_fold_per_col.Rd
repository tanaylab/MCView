% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcview_kernels.R
\name{top_fold_per_col}
\alias{top_fold_per_col}
\title{Top-K per column of a fold-point matrix with marker thresholds}
\usage{
top_fold_per_col(fp, k, min_val, fold_reg, use_abs)
}
\arguments{
\item{fp}{Numeric matrix (genes x metacells) with rownames and colnames.}

\item{k}{Positive integer -- rows returned per column.}

\item{min_val}{Mask values `< min_val` after adding `fold_reg`.}

\item{fold_reg}{Constant added to every non-NA value before masking.}

\item{use_abs}{Rank by `abs(x)` when TRUE, else by raw `x`.}
}
\value{
tibble with columns `(metacell, gene, rank, fp)` of length
  `k * ncol(fp)`. Rows where fewer than `k` values qualified receive
  `NA` for `gene` and `fp`.
}
\description{
Replaces the R `select_top_fold_genes_per_metacell` loop with a
single-pass cpp11 + OpenMP kernel.
}
