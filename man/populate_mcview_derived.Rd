% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/daf_cache_populate.R
\name{populate_mcview_derived}
\alias{populate_mcview_derived}
\alias{populate_mcview_cache}
\title{Populate MCView derived data for a DAF dataset}
\usage{
populate_mcview_derived(
  daf_path,
  cache_dir = ".mcview_derived",
  cache_type = "files",
  what = NULL,
  force = FALSE,
  verbose = TRUE
)

populate_mcview_cache(
  daf_path,
  cache_dir = ".mcview_derived",
  cache_type = "files",
  what = NULL,
  force = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{daf_path}{Path to base DAF (directory for files_daf, or H5 file)}

\item{cache_dir}{Derived data directory path (absolute or relative to daf_path)}

\item{cache_type}{Storage type: "files" (persistent) or "memory" (session only)}

\item{what}{What to compute: NULL (all), or character vector subset of:
"correlations", "metacell_top_genes", "gene_stats", "type_markers"}

\item{force}{Recompute even if derived data exists and is valid}

\item{verbose}{Print progress messages}
}
\value{
Invisibly returns the derived path (for files) or TRUE (for memory)
}
\description{
This function can be called standalone (outside MCView) to precompute
derived data. Useful for CI/CD pipelines, batch processing, or scheduled jobs.
}
\examples{
\dontrun{
# Populate all derived data
populate_mcview_derived("/path/to/daf")

# Populate specific items
populate_mcview_derived(
    "/path/to/daf",
    what = c("correlations", "gene_stats"),
    verbose = TRUE
)

# Use custom derived directory
populate_mcview_derived(
    "/path/to/daf",
    cache_dir = "/shared/derived/my_dataset"
)
}
}
