% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcview_kernels.R
\name{streaming_top_k_cor}
\alias{streaming_top_k_cor}
\title{Streaming top-K Pearson correlation (block gemm + per-row heap)}
\usage{
streaming_top_k_cor(
  X,
  k,
  diag = FALSE,
  min_cor = 0,
  block_rows = getOption("mcview.streaming_block_rows", 512L)
)
}
\arguments{
\item{X}{Numeric matrix (genes × metacells).}

\item{k}{Positive integer — top-K retained per sign per gene1.}

\item{diag}{Include self-correlations? Default FALSE.}

\item{min_cor}{Drop any `|cor| < min_cor`. Default 0.}

\item{block_rows}{Tile size. Default `getOption("mcview.streaming_block_rows", 512L)`.}
}
\value{
tibble with columns `(gene1, gene2, cor, type)` of length up
  to `2 * k * nrow(X)`.
}
\description{
Replaces the full G×G correlation materialization in
`calc_gg_mc_top_cor`. Tiles row-blocks of size `block_rows` and
maintains per-row bounded heaps for positive and negative
correlations. Never holds the full cor matrix in memory.
}
