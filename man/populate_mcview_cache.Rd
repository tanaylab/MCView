% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/daf_cache.R
\name{populate_mcview_cache}
\alias{populate_mcview_cache}
\title{Populate MCView cache for a DAF dataset}
\usage{
populate_mcview_cache(
  daf_path,
  cache_dir = ".mcview_cache",
  cache_type = "files",
  what = NULL,
  force = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{daf_path}{Path to base DAF (directory for files_daf, or H5 file)}

\item{cache_dir}{Cache directory path (absolute or relative to daf_path)}

\item{cache_type}{Cache type: "files" (persistent) or "memory" (session only)}

\item{what}{What to cache: NULL (all), or character vector subset of:
"correlations", "metacell_top_genes", "gene_stats"}

\item{force}{Recompute even if cache exists and is valid}

\item{verbose}{Print progress messages}
}
\value{
Invisibly returns the cache path (for files) or TRUE (for memory)
}
\description{
This function can be called standalone (outside MCView) to precompute
cached data. Useful for CI/CD pipelines, batch processing, or scheduled jobs.
}
\examples{
\dontrun{
# Populate all cache data
populate_mcview_cache("/path/to/daf")

# Populate specific cache items
populate_mcview_cache(
    "/path/to/daf",
    what = c("correlations", "gene_stats"),
    verbose = TRUE
)

# Use custom cache directory
populate_mcview_cache(
    "/path/to/daf",
    cache_dir = "/shared/cache/my_dataset"
)
}
}
